const routes = {
    home: '/',
    chart: '/chart',
    filter: '/filter/:objectType/:timeFrom/:timeTo/:sensorID/:status',
    chartFilter: '/chart/filter/:objectType/:timeFrom/:timeTo/:sensorID/:status',
};

export default routes;
